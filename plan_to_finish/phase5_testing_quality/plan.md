# 🧪 المرحلة 5: الاختبارات وجودة الكود

## الهدف
رفع جودة الكود وإضافة تغطية اختبارية تضمن استقرار المشروع على المدى الطويل.

## الأولوية: مهم 🟡
## الجهد المقدر: 2-3 أيام

---

## المهمة 5.1: اختبارات وحدة للباك إند (Unit Tests)

### الحالة الحالية
- ملف واحد فقط: `products.service.spec.ts`
- ملف E2E أساسي: `app.e2e-spec.ts`

### خطوات التنفيذ
1. كتابة اختبارات لـ `CategoriesService`:
   - اختبار `findAll()`
   - اختبار `create()` مع category موجودة
   - اختبار `remove()` مع فئة مرتبطة بمنتجات
   - اختبار `remove()` مع فئة فرعية

2. كتابة اختبارات لـ `ProductsService`:
   - اختبار `findAll()` مع فلتر `hasImages`
   - اختبار `findAll()` مع بحث `q`
   - اختبار `create()` مع بيانات صحيحة
   - اختبار `remove()` مع منتج مرتبط بصور

3. كتابة اختبارات لـ `HealthService`:
   - اختبار `getReadiness()` عند اتصال MongoDB
   - اختبار `getReadiness()` عند انقطاع MongoDB
   - اختبار `checkRedis()` عند اتصال Redis

4. كتابة اختبارات لـ `FoldersService`:
   - اختبار إنشاء مجلد
   - اختبار إعادة تسمية
   - اختبار حذف مجلد

5. كتابة اختبارات لـ `AuthService`:
   - اختبار تسجيل الدخول بنجاح
   - اختبار تسجيل الدخول بكلمة مرور خاطئة
   - اختبار مستخدم غير موجود

---

## المهمة 5.2: اختبارات E2E للباك إند

### خطوات التنفيذ
1. اختبار تدفق المصادقة كاملاً
2. اختبار CRUD للفئات
3. اختبار CRUD للمنتجات
4. اختبار حماية المسارات الإدارية
5. اختبار Health endpoints

---

## المهمة 5.3: اختبارات الفرونت إند

### خطوات التنفيذ
1. اختبار `CatalogPage` - مزامنة الفلاتر مع URL
2. اختبار `Filters` component - تغيير الفلاتر
3. اختبار `SearchBar` - إدخال البحث
4. اختبار `SEO` component - meta tags
5. اختبار `AuthContext` - تسجيل الدخول/الخروج

---

## المهمة 5.4: تقسيم ProductManagement.tsx

### المشكلة
ملف `ProductManagement.tsx` حجمه 154 KB وهو كبير جداً.

### خطوات التنفيذ
1. تحليل المكونات داخل الملف
2. استخراج مكونات فرعية:
   - `ProductTable.tsx` - جدول المنتجات
   - `ProductFilters.tsx` - فلاتر المنتجات
   - `ProductForm.tsx` - نموذج إنشاء/تعديل
   - `ProductImageManager.tsx` - إدارة صور المنتج
   - `ProductActions.tsx` - أزرار الإجراءات
3. استخراج custom hooks:
   - `useProductManagement.ts` - منطق الإدارة
   - `useProductFilters.ts` - منطق الفلاتر
4. اختبار كل مكون بشكل مستقل

---

## المهمة 5.5: مراجعة توافق الأنواع (Types)

### المشكلة
عدم تطابق بين بعض أنواع الفرونت والباك.

### خطوات التنفيذ
1. مقارنة `Product` type مع response الفعلي
2. مقارنة `Category` type مع response الفعلي
3. إصلاح `PaginatedResponse` ليتطلب `totalItems`
4. إصلاح `withoutImagesCount` من intersection إلى type واضح
5. إضافة type guards حيث يلزم
6. تشغيل `npm run type-check` والتأكد من عدم وجود أخطاء

---

## المهمة 5.6: إصلاح ESLint Warnings

### خطوات التنفيذ
1. تشغيل `npm run lint` في الباك إند
2. تشغيل eslint في الفرونت إند
3. إصلاح جميع warnings
4. إضافة قواعد ESLint مشددة اختيارياً

---

## المهمة 5.7: تحسين i18n

### المشكلة
معظم النصوص مكتوبة مباشرة بالعربية ولا تستخدم نظام الترجمة.

### خطوات التنفيذ
1. مراجعة ملفات الترجمة الحالية
2. استخراج النصوص العربية الثابتة إلى ملفات ترجمة
3. استبدال النصوص بـ `t('key')` في المكونات الأساسية
4. (اختياري) إضافة دعم اللغة الإنجليزية
